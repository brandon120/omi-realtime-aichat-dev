# OpenAI API Configuration
# Prefer OPENAI_API_KEY (SDK default); OPENAI_KEY is still supported
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_KEY=

# Omi API Configuration (Updated)
OMI_APP_ID=your_omi_app_id_here
OMI_APP_SECRET=your_omi_app_secret_here

# Server Configuration (optional - defaults to 3000)
PORT=3000
## Comma-separated list of allowed web origins for CORS, e.g.:
## http://localhost:8081,https://yourapp.web.app
## Include your Netlify domain below to allow the hosted web app to call the API
CORS_ORIGINS=http://localhost:8081,https://omi-ai-realtime-chat-app.netlify.app

## Optional: wildcard domain patterns (comma-separated). Supports forms like:
## https://*.netlify.app or http://*.localhost
CORS_ORIGIN_PATTERNS=

# Database (Prisma / Postgres)
# Example Railway URL: postgres://user:password@host:port/dbname
DATABASE_URL=

# Feature flags
# Set to true to enable DB/auth/user system features in dev
ENABLE_USER_SYSTEM=false

# New OMI features & rollout
ENABLE_CONTEXT_ACTIVATION=true
ENABLE_NEW_OMI_ROUTES=true
ENABLE_PROMPT_WORKERS=false
QUIET_HOURS_ENABLED=true

# Auth/session
SESSION_SECRET=change_me_in_production
